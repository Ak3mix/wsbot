# Copia este archivo como .env y descomenta/edita lo que necesites.
# Si no defines nada, se usan los defaults del bot.

# DEBUG=true/false (por defecto false; los marcadores de una línea siempre se muestran)
# DEBUG_MODE=true

# CSV de usuarios autorizados (vacío = todos)
# ALLOWED_USERS=521234567890,521234567891@c.us,2839174721783@lid

# CSV de grupos autorizados (vacío = todos)
# ALLOWED_GROUPS=120363427961804581@g.us

# CSV de palabras clave
# KEYWORDS=aeropuerto,terminal 1,terminal 2,terminal 3

# CSV de respuestas variadas
# AUTO_REPLIES=Yo, estoy al tanto.,Sí, ya lo sé,Enterado,sin problema

# (Legacy) Respuesta única
# AUTO_REPLY=Yo, estoy al tanto.

# Ruta de la carpeta de sesión (Baileys). Por defecto: .baileys_auth
# SESSION_PATH=.baileys_auth

# Token para proteger el endpoint /qr (opcional)
# QR_TOKEN=mi-token-secreto

# Token dedicado para /restart (si falta usa QR_TOKEN)
# RESTART_TOKEN=mi-token-restart

# Token dedicado para /logs (si falta usa QR_TOKEN)
# LOGS_TOKEN=mi-token-logs

# Delay de respuesta en ms (0 = instantáneo)
# REPLY_DELAY_MIN=0
# REPLY_DELAY_MAX=0

# Notificaciones y control vía Telegram (push al móvil / grupo)
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# TELEGRAM_CHAT_ID=-1004326942660

# CSV de ids de Telegram autorizados a ejecutar comandos de control
# (vacío = solo /help). Tuyo + la otra persona si quieres
# TELEGRAM_ALLOWED_IDS=1533493990,222222222

# Avisar al grupo cada vez que el bot responde (solo nombre del grupo). true/false
# NOTIFY_REPLY=false

# Puerto del web server (Render lo inyecta automáticamente)
# PORT=7860
